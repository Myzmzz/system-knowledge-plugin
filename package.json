{
  "name": "system-knowledge-plugin",
  "version": "0.1.0",
  "description": "System knowledge graph infrastructure for agent-collaborative development — feature dependency graph, impact analysis, business-chain test paths. Ships as both a Claude Code plugin and a Codex plugin.",
  "type": "module",
  "private": true,
  "license": "MIT",
  "author": "Myzmzz",
  "bin": {
    "knowledge": "cli/index.ts"
  },
  "scripts": {
    "build": "node scripts/build.mjs",
    "typecheck": "tsc --noEmit",
    "test": "vitest run",
    "test:watch": "vitest",
    "knowledge": "tsx cli/index.ts",
    "mcp": "tsx mcp-server/src/index.ts"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.0",
    "yaml": "^2.5.1",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "esbuild": "^0.24.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0",
    "vitest": "^2.1.0"
  },
  "engines": {
    "node": ">=18"
  }
}
