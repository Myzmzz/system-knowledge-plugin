node_modules/
dist/
*.log
.DS_Store
coverage/

# Generated reports (regenerated by `knowledge graph` / `knowledge impact`)
reports/
examples/**/reports/

# Draft knowledge writes (per-machine, not committed)
knowledge/.drafts/
examples/**/knowledge/.drafts/

# NOTE: mcp/index.js (the esbuild bundle) IS committed on purpose —
# marketplace install = git clone, so the runnable MCP server must be in-tree.
